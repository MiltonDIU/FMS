# Legacy → New System Mapping (Final)

**Faculty Management System (FMS)**
**Milestone 1 – Planning & Design**

---

> ## Reconciliation note — 2026-08-30
>
> This is a **Milestone-1 baseline document**, written before development and approved as the plan. It is kept unchanged in intent as the record of what was agreed. Since it was written, the system has been built, so the points below have been corrected in place and the current description now lives in:
>
> - *BRD - Faculty Management System.md* (revision 2.1)
> - *SRS - Faculty Management System.md* (revision 2.1)
>
> **Where this document diverged from the built system:**
>
> - **Five target table names in the mapping were provisional and differ from what was built:** `research` is `research_projects`, `previous_employments` is `job_experiences`, `teacher_roles` is `administrative_role_user`, `drafts` + `approval` are `teacher_versions` + `approval_settings`, and `audit_logs` is `activity_log` (Spatie ActivityLog).
> - **ERP integration is no longer "not available".** The client is built and has been run against the live DIU HR directory; only production credentials are outstanding.
> - **The migration actually happened, and the volumes are known.** The seven legacy free-text columns produced **34,300 individual records**. Counts per table are listed below.
> - **Three structures were added that the mapping did not anticipate:** `research_interests`, `publication_authors` (with author role, affiliation and incentive amount) and the Scopus review tables `scopus_imports` and `scopus_author_ids`.

---


## 1. Purpose

This document maps the data fields, modules, and workflows from the **existing legacy system** to the **new Faculty Management System (FMS)**. It ensures clear traceability of features, reduces data migration errors, and guides the development and ERD design process.

---

## 2. Mapping Table

| Legacy System Module / Field             | Legacy Data Type / Notes                            | New FMS Module / Table / Field                   | Comments / Transformation Notes                         |
| ---------------------------------------- | --------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------- |
| Department Account Login                 | Department user and password                        | `users` + `roles` + `department_id`              | Admin/ Teacher login maintained via RBAC/               |
| Teacher Personal Information             | Collect to PDF / DOC and Admin entry teachers table | `teachers` table: `name`, `dob`, `email`, etc.   | Converted to structured fields                          |
| Academic Qualification                   | Text field                                          | `educations` table                               | Multi-row, normalized — **1,831 records produced**       |
| Training Experience                      | Text field                                          | `training_experiences` table                     | Multi-row, normalized — **5,032 records produced**       |
| Teaching Area                            | Text field                                          | `teaching_areas` table                           | Multi-row normalized — **4,584 records produced**        |
| Publications (Books, Journals, Articles) | Text field / PDF                                    | `publications` + `publication_authors` tables    | Multi row normalized — **17,510 publications, 25,485 author attributions** |
| Research Projects                        | Text field                                          | `research_projects` table (*named `research` in this draft*) | Multi row normalized — module built, **no records entered yet** |
| Memberships & Professional Affiliations  | Text field                                          | `memberships` table                              | Multi-entry allowed — **905 records produced**           |
| Awards & Scholarships                    | Text / List                                         | `awards` table                                   | Normalized for reporting — **2,486 records produced**    |
| Previous Employment                      | Text field                                          | `job_experiences` table (*named `previous_employments` in this draft*) | Multi-row, structured — **1,952 records produced** |
| Profile Updates / Approvals              | Manual PDF / DOC review                             | `teacher_versions` + `approval_settings` (*named `drafts` + `approval` in this draft*) | Version control, configured per section — 15 sections |
| Role Assignments                         | Department-only admin                               | `roles` + `administrative_role_user` (*named `teacher_roles` in this draft*) | Multiple roles per user, audit trail — 9 roles, 11 administrative roles |
| Statistics / Reporting                   | None / manual                                       | Dashboards and statistics modules                | Faculty/Department/Teacher-level reports                |
| ERP Integration                          | Not available                                       | DIU HR/ERP API — token, employee search, employee profile by ID | **Built and exercised**; auto-fills the new teacher form. Awaiting production credentials |
| Public Profile Display                   | single profile / outdated design                    | `teachers` module                                | Only approved versions visible                          |
| Audit Trail                              | No logs                                             | `activity_log` table, Spatie ActivityLog (*named `audit_logs` in this draft*) | All updates, approvals, rollbacks logged — **2,017 entries** |
| *(not anticipated)* Research interests   | Part of the profile prose                           | `research_interests` table                       | Added during build — **854 records**                    |
| *(not anticipated)* Scopus intake        | Not available                                       | `scopus_imports`, `scopus_author_ids`            | Added during build — a Scopus CSV export is reviewed and merged under review |

---

## 3. Notes

1. **Normalization:** All previously unstructured text fields are converted into normalized tables to enable reporting and version tracking.
2. **Versioning:** Publications, profile updates, and experiences maintain full version history with rollback capability.
3. **Role-Based Access Control (RBAC):** Mapping considers multiple roles per user and admin approval requirements.
4. **ERP Integration:** Data mapping includes a placeholder for Phase-2 ERP
   fields. *As built, the mapping is no longer a placeholder: 83 field rules are
   configured against the vendor's field names and are maintained in the panel.*
5. **Public vs Internal Data:** Only verified teachers’ information is publicly viewable; student or unknown contributor data is stored internally for reference.

---

This document will act as the reference for:

* Database schema (ERD) design
* Workflow & approval system implementation
* Data migration strategy from legacy to new FMS

---

**Document Status:** Milestone-1 mapping baseline, annotated on 2026-08-30 with
what each field actually became. The migration is complete; the mechanism is
described in *SRS - Faculty Management System.md* §5.3.
